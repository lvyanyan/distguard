# Rule catalog

<!-- Auto-generated by `pnpm rules:doc` — do not edit by hand. -->

26 rules ship with this version. Every finding links to remediation guidance; severity drives the `--fail-on` gate.

| rule id | severity | category | detects |
| --- | --- | --- | --- |
| `alibaba-accesskey-id` | **critical** | credential | Alibaba Cloud access key id embedded in build output |
| `aws-access-key-id` | **critical** | credential | AWS access key id embedded in build output |
| `azure-storage-account-key` | **critical** | credential | Azure Storage account key embedded in build output |
| `github-token` | **critical** | credential | GitHub token embedded in build output |
| `gitlab-pat` | **critical** | credential | GitLab personal access token embedded in build output |
| `google-oauth-client-secret` | **critical** | credential | Google OAuth client secret embedded in build output |
| `npm-access-token` | **critical** | credential | npm access token embedded in build output |
| `private-key-block` | **critical** | credential | Private key material embedded in build output |
| `stripe-live-secret-key` | **critical** | credential | Stripe live-mode secret key embedded in build output |
| `anthropic-api-key` | **high** | credential | Anthropic API key embedded in build output |
| `basic-auth-url` | **high** | credential | URL contains inline basic-auth credentials |
| `digitalocean-token` | **high** | credential | DigitalOcean personal access token embedded in build output |
| `discord-bot-token` | **high** | credential | Discord bot token embedded in build output |
| `google-api-key` | **high** | credential | Google API key embedded in build output |
| `openai-secret-key` | **high** | credential | OpenAI secret key embedded in build output |
| `sendgrid-api-key` | **high** | credential | SendGrid API key embedded in build output |
| `shopify-custom-app-token` | **high** | credential | Shopify custom app access token embedded in build output |
| `slack-token` | **high** | credential | Slack token embedded in build output |
| `telegram-bot-token` | **high** | credential | Telegram bot token embedded in build output |
| `twilio-api-key` | **high** | credential | Twilio-style API key embedded in build output |
| `firebase-database-url` | **medium** | exposure | Firebase Realtime Database URL referenced in build output |
| `hardcoded-jwt` | **medium** | credential | JSON Web Token embedded in build output |
| `huggingface-token` | **medium** | credential | Hugging Face access token embedded in build output |
| `sourcemap-restorable-sources` | **medium** | exposure | Published source map ships original sourcesContent |
| `internal-network-url` | **low** | exposure | Private/internal network URL referenced in build output |
| `sourcemap-exposed` | **low** | exposure | Source map published alongside production bundle |
